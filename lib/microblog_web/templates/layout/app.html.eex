<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Microblog!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>

    <nav class="navbar navbar-dark navbar-expand-md bg-darkgreen">
      <%= link "The Void", to: page_path(@conn, :index), class: "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <%= if @current_user do %>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <span class="text-light mx-1">
            Welcome, <%= @current_user.username %>.
          </span>
          <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li>  </li>
            <li class="nav-item active">
              <%= link "New Post", to: post_path(@conn, :new), class: "nav-link" %>
            </li>
            <li class="nav-item active">
              <%= link "Profile", to: user_path(@conn, :show, @current_user), class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link "Logout", to: session_path(@conn, :logout), method: :delete, class: "nav-link" %>
            </li>
          </ul>
      <% end %>

      <form class="form-inline">
        <input type="text" placeholder="Search the void" class="form-control">
        <button class="btn btn-light my-2 my-sm-0 m-1" type="submit">Do it</button>
      </form>
        </div>
    </nav>

    <div class="container">
      <!-- if/end lines below taken from nu_mart notes -->
      <%= if get_flash(@conn, :info) do %>
        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <% end %>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
